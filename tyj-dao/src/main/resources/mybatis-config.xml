<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

<!--<plugins> 可以添加 sql 语句的拦截器，例如做  数据库分页插件，只拦截查询语句.
		<plugin interceptor=""/>
	</plugins>-->
	<!--该属性可以给包中的类注册别名，注册后在 XXX_mapper.xml 中可以直接使用类名，而不用使用全限定的类名（就是不用包含包名）。
	参考 http://blog.csdn.net/fxq8866/article/details/52635153?foxhandler=RssReadRenderProcessHandler -->
	<typeAliases>
		<package name="cn.tyj.dao.pojo" />
		<package name="cn.tyj.dao.vo" />
	</typeAliases>
</configuration>
